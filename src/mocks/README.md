# mocks

**Dados fictícios de vitrine** (commitados de propósito, `isMock: true` em todo
item). Mínimo 3 produtos por categoria. Consumidos apenas via
`MockDealsRepository` — nunca importados pela UI. Ver skill `ght-mock-data`.
